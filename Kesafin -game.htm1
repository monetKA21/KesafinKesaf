<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kesafin - Открытие кейсов</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            text-align: center;
            padding: 20px 0;
            margin-bottom: 30px;
            border-bottom: 2px solid #0f3460;
        }

        .logo {
            font-size: 3.5rem;
            font-weight: 800;
            background: linear-gradient(45deg, #e94560, #533483);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: 0 0 15px rgba(233, 69, 96, 0.3);
            margin-bottom: 10px;
        }

        .tagline {
            font-size: 1.2rem;
            color: #a9b7c6;
            margin-bottom: 20px;
        }

        .balance {
            background: rgba(15, 52, 96, 0.7);
            padding: 10px 20px;
            border-radius: 10px;
            display: inline-block;
            font-size: 1.3rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        .balance span {
            color: #e94560;
            font-weight: bold;
        }

        nav {
            display: flex;
            justify-content: center;
            margin-bottom: 30px;
            background: rgba(15, 52, 96, 0.5);
            border-radius: 10px;
            padding: 10px;
        }

        .nav-btn {
            background: none;
            border: none;
            color: #fff;
            padding: 10px 20px;
            margin: 0 10px;
            font-size: 1.1rem;
            cursor: pointer;
            border-radius: 5px;
            transition: all 0.3s ease;
        }

        .nav-btn.active {
            background: #e94560;
            box-shadow: 0 0 10px rgba(233, 69, 96, 0.5);
        }

        .nav-btn:hover {
            background: rgba(233, 69, 96, 0.3);
        }

        .section {
            display: none;
            animation: fadeIn 0.5s ease;
        }

        .section.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .cases-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .case-card {
            background: rgba(15, 52, 96, 0.7);
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .case-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
        }

        .case-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, #e94560, #533483);
        }

        .case-name {
            font-size: 1.3rem;
            margin-bottom: 10px;
            color: #fff;
        }

        .case-price {
            font-size: 1.1rem;
            color: #e94560;
            margin-bottom: 15px;
        }

        .case-items {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 15px;
        }.item-preview {
            width: 30px;
            height: 30px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
        }

        .common { border: 1px solid #5d8aa8; }
        .uncommon { border: 1px solid #2e8b57; }
        .rare { border: 1px solid #1e90ff; }
        .epic { border: 1px solid #9370db; }
        .legendary { border: 1px solid #ff8c00; }

        .case-open-area {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            display: none;
        }

        .case-display {
            width: 300px;
            height: 300px;
            background: rgba(15, 52, 96, 0.9);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 30px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 0 30px rgba(233, 69, 96, 0.5);
        }

        .item-revealed {
            font-size: 4rem;
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }

        .item-name {
            font-size: 2rem;
            margin-bottom: 10px;
            text-align: center;
        }

        .item-rarity {
            font-size: 1.5rem;
            margin-bottom: 20px;
            text-align: center;
        }

        .close-case {
            background: #e94560;
            border: none;
            color: white;
            padding: 10px 30px;
            border-radius: 5px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .close-case:hover {
            background: #c2334d;
        }

        .inventory-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .inventory-item {
            background: rgba(15, 52, 96, 0.7);
            border-radius: 10px;
            padding: 15px;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: transform 0.3s ease;
        }

        .inventory-item:hover {
            transform: translateY(-5px);
        }

        .item-icon {
            font-size: 3rem;
            margin-bottom: 10px;
        }

        .item-details {
            width: 100%;
        }

        .item-title {
            font-size: 1.1rem;
            margin-bottom: 5px;
        }

        .item-rarity {
            font-size: 0.9rem;
            margin-bottom: 10px;
        }

        .item-price {
            font-size: 1rem;
            color: #e94560;
            margin-bottom: 10px;
        }

        .sell-btn {
            background: #e94560;
            border: none;
            color: white;
            padding: 5px 15px;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s ease;
            width: 100%;
        }

        .sell-btn:hover {
            background: #c2334d;
        }

        .empty-inventory {
            text-align: center;
            padding: 40px;
            font-size: 1.2rem;
            color: #a9b7c6;
        }

        .rarity-common { color: #5d8aa8; }
        .rarity-uncommon { color: #2e8b57; }
        .rarity-rare { color: #1e90ff; }
        .rarity-epic { color: #9370db; }
        .rarity-legendary { color: #ff8c00; }footer {
            text-align: center;
            margin-top: 50px;
            padding: 20px;
            color: #a9b7c6;
            border-top: 1px solid #0f3460;
        }

        /* Анимация падения предмета */
        @keyframes fallDown {
            0% { transform: translateY(-100px) rotate(0deg); opacity: 0; }
            70% { transform: translateY(20px) rotate(180deg); opacity: 1; }
            100% { transform: translateY(0) rotate(360deg); opacity: 1; }
        }

        .fall-animation {
            animation: fallDown 1s ease forwards;
        }

        /* Адаптивность */
        @media (max-width: 768px) {
            .cases-grid, .inventory-grid {
                grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            }

            .logo {
                font-size: 2.5rem;
            }

            .case-display {
                width: 250px;
                height: 250px;
            }

            .item-revealed {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">KESAFIN</div>
            <div class="tagline">Открывай кейсы и получай уникальные предметы</div>
            <div class="balance">Баланс: <span id="balance">1000</span> ₽</div>
        </header>

        <nav>
            <button class="nav-btn active" data-section="cases">Кейсы</button>
            <button class="nav-btn" data-section="inventory">Инвентарь</button>
        </nav>

        <section id="cases" class="section active">
            <h2>Доступные кейсы</h2>
            <div class="cases-grid" id="cases-container">
                <!-- Кейсы будут добавлены через JavaScript -->
            </div>
        </section>

        <section id="inventory" class="section">
            <h2>Ваш инвентарь</h2>
            <div class="inventory-grid" id="inventory-container">
                <!-- Предметы инвентаря будут добавлены через JavaScript -->
            </div>
        </section>

        <div class="case-open-area" id="case-open-area">
            <div class="case-display">
                <div class="item-revealed" id="item-revealed">📱</div>
            </div>
            <div class="item-name" id="revealed-item-name">Предмет</div>
            <div class="item-rarity" id="revealed-item-rarity">Обычный</div>
            <button class="close-case" id="close-case">Закрыть</button>
        </div>

        <footer>
            <p>Kesafin &copy; 2023. Все права защищены.</p>
        </footer>
    </div>

    <script>
        // Данные кейсов
        const cases = [
            {
                id: 1,
                name: "Базовый кейс",
                price: 100,
                items: [
                    { name: "Кнопочный телефон", rarity: "common", icon: "📱", price: 50 },
                    { name: "Радиотелефон", rarity: "common", icon: "📞", price: 60 },
                    { name: "Старый смартфон", rarity: "uncommon", icon: "📲", price: 150 },
                    { name: "Планшет", rarity: "rare", icon: "📟", price: 300 },
                    { name: "Ноутбук", rarity: "epic", icon: "💻", price: 800 },
                    { name: "Игровой ПК", rarity: "legendary", icon: "🖥️", price: 2000 }
                ]
            },
            {
                id: 2,
                name: "Техно кейс",
                price: 250,
                items: [
                    { name: "Умные часы", rarity: "uncommon", icon: "⌚", price: 200 },
                    { name: "Беспроводные наушники", rarity: "uncommon", icon: "🎧", price: 250 },
                    { name: "Фитнес-браслет", rarity: "rare", icon: "📿", price: 400 },
                    { name: "Портативная колонка", rarity: "rare", icon: "🔊", price: 500 },{ name: "Игровая консоль", rarity: "epic", icon: "🎮", price: 1500 },
                    { name: "Виртуальная реальность", rarity: "legendary", icon: "🥽", price: 3000 }
                ]
            },
            {
                id: 3,
                name: "Премиум кейс",
                price: 500,
                items: [
                    { name: "Смартфон среднего класса", rarity: "rare", icon: "📱", price: 600 },
                    { name: "Планшет премиум", rarity: "rare", icon: "📟", price: 800 },
                    { name: "Ультрабук", rarity: "epic", icon: "💻", price: 1200 },
                    { name: "Флагманский смартфон", rarity: "epic", icon: "📲", price: 1500 },
                    { name: "Мощный игровой ноутбук", rarity: "legendary", icon: "💻", price: 2500 },
                    { name: "Apple iPhone Pro", rarity: "legendary", icon: "📱", price: 3500 }
                ]
            },
            {
                id: 4,
                name: "Геймерский кейс",
                price: 400,
                items: [
                    { name: "Игровая мышь", rarity: "common", icon: "🖱️", price: 100 },
                    { name: "Игровая клавиатура", rarity: "uncommon", icon: "⌨️", price: 300 },
                    { name: "Игровой монитор", rarity: "rare", icon: "🖥️", price: 800 },
                    { name: "Игровой процессор", rarity: "epic", icon: "⚙️", price: 1200 },
                    { name: "Видеокарта", rarity: "epic", icon: "🎴", price: 2000 },
                    { name: "Полный игровой комплект", rarity: "legendary", icon: "🎮", price: 4000 }
                ]
            },
            {
                id: 5,
                name: "Аксессуары кейс",
                price: 150,
                items: [
                    { name: "Чехол для телефона", rarity: "common", icon: "📱", price: 30 },
                    { name: "Защитное стекло", rarity: "common", icon: "🔍", price: 40 },
                    { name: "Power Bank", rarity: "uncommon", icon: "🔋", price: 120 },
                    { name: "Кабель быстрой зарядки", rarity: "uncommon", icon: "🔌", price: 80 },
                    { name: "Беспроводная зарядка", rarity: "rare", icon: "⚡", price: 200 },
                    { name: "Умный держатель", rarity: "epic", icon: "📱", price: 350 }
                ]
            },
            {
                id: 6,
                name: "Элитный кейс",
                price: 1000,
                items: [
                    { name: "Apple MacBook", rarity: "epic", icon: "💻", price: 2000 },
                    { name: "Samsung Galaxy Ultra", rarity: "epic", icon: "📱", price: 1800 },
                    { name: "Игровой монитор 4K", rarity: "epic", icon: "🖥️", price: 2500 },
                    { name: "Профессиональная камера", rarity: "legendary", icon: "📷", price: 3500 },
                    { name: "Apple iPhone Pro Max", rarity: "legendary", icon: "📱", price: 4000 },
                    { name: "Игровой компьютер Ultimate", rarity: "legendary", icon: "🖥️", price: 6000 }
                ]
            }
        ];

        // Данные пользователя
        let userBalance = 1000;
        let userInventory = [];

        // Инициализация при загрузке страницы
        document.addEventListener('DOMContentLoaded', function() {
            // Отображение кейсов
            renderCases();

            // Отображение инвентаря
            renderInventory();

            // Навигация
            setupNavigation();

            // Обработчик закрытия кейса
            document.getElementById('close-case').addEventListener('click', closeCase);
        });

        // Функция отображения кейсов
        function renderCases() {
            const casesContainer = document.getElementById('cases-container');casesContainer.innerHTML = '';

            cases.forEach(caseItem => {
                const caseCard = document.createElement('div');
                caseCard.className = 'case-card';
                caseCard.innerHTML =
                    <div class="case-name">${caseItem.name}</div>
                    <div class="case-price">${caseItem.price} ₽</div>
                    <div class="case-items">
                        ${caseItem.items.map(item =>
                            <div class="item-preview ${item.rarity}">${item.icon}</div>
                        ).join('')}
                    </div>
                ;

                caseCard.addEventListener('click', () => openCase(caseItem));
                casesContainer.appendChild(caseCard);
            });
        }

        // Функция открытия кейса
        function openCase(caseItem) {
            if (userBalance < caseItem.price) {
                alert('Недостаточно средств для открытия этого кейса!');
                return;
            }

            // Списываем стоимость кейса
            userBalance -= caseItem.price;
            updateBalance();

            // Выбираем случайный предмет из кейса с учетом редкости
            const weights = {
                common: 40,
                uncommon: 30,
                rare: 15,
                epic: 10,
                legendary: 5
            };

            // Создаем взвешенный массив предметов
            const weightedItems = [];
            caseItem.items.forEach(item => {
                const weight = weights[item.rarity];
                for (let i = 0; i < weight; i++) {
                    weightedItems.push(item);
                }
            });

            // Выбираем случайный предмет
            const randomIndex = Math.floor(Math.random() * weightedItems.length);
            const wonItem = {...weightedItems[randomIndex]};

            // Добавляем предмет в инвентарь
            userInventory.push(wonItem);

            // Показываем анимацию открытия
            showCaseOpening(wonItem);

            // Обновляем инвентарь
            renderInventory();
        }

        // Функция показа анимации открытия кейса
        function showCaseOpening(item) {
            const caseOpenArea = document.getElementById('case-open-area');
            const itemRevealed = document.getElementById('item-revealed');
            const itemName = document.getElementById('revealed-item-name');
            const itemRarity = document.getElementById('revealed-item-rarity');

            // Устанавливаем данные о выигранном предмете
            itemRevealed.textContent = item.icon;
            itemName.textContent = item.name;
            itemRarity.textContent = getRarityText(item.rarity);
            itemRarity.className = item-rarity rarity-${item.rarity};

            // Добавляем класс для анимации
            itemRevealed.classList.add('fall-animation');

            // Воспроизводим звук открытия (заглушка)
            playOpenSound();

            // Показываем область открытия
            caseOpenArea.style.display = 'flex';

            // Убираем анимацию после завершения
            setTimeout(() => {
                itemRevealed.classList.remove('fall-animation');
            }, 1000);
        }

        // Функция закрытия кейса
        function closeCase() {
            const caseOpenArea = document.getElementById('case-open-area');
            caseOpenArea.style.display = 'none';
        }

        // Функция отображения инвентаря
        function renderInventory() {
            const inventoryContainer = document.getElementById('inventory-container');inventoryContainer.innerHTML = '';

            if (userInventory.length === 0) {
                inventoryContainer.innerHTML = '<div class="empty-inventory">Ваш инвентарь пуст</div>';
                return;
            }

            userInventory.forEach((item, index) => {
                const inventoryItem = document.createElement('div');
                inventoryItem.className = 'invent
